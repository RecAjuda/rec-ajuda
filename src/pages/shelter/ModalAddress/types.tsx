import LogoArrow from "../../../assets/shelter/arrow.svg";

export const shelterIcons: Record<string, string> = {
    arrow: LogoArrow
}

export type ModalAddressProps = {
    street: string;
    cep: string;
  };
  